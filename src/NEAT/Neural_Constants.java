package NEAT;
public class Neural_Constants {
    public static final int NUM_OF_INPUTS = 3;

    public static final double DIFFERENTIAL = 1.0; // The range at which the neural network weights + biases can change during a mutation
}
